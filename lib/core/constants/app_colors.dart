import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0C0E0D);

  static const Color primaryBlue = Color(0xFF0A9FF5);

  static const Color unselectedItem = Color(0xFF2C2D2E);

  static const Color white = Colors.white;

  static const Color textSecondary = Color(0xFF9EA3A6);
 
  // Translucent white used for glass-style surfaces (search bar, header
  // icon button) and thin dividers between card stats.
  static const Color surface = Color(0x1AFFFFFF);
  static const Color divider = Color(0x33FFFFFF);
 
  // Solid footer background for property cards (sits below the photo,
  // not overlapping it).
  static const Color cardFooter = Color(0xFF15171A);
}